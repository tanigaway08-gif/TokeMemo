<% content_for :hide_hamburger, true %>

<div class="container-fluid mt-4 px-3">
  <!-- 不要な余白を防ぐため m-0 を追加 -->
  <div class="row m-0">
    <!-- col-md-6 を削除し、col-12 だけで全幅を使う -->
    <div class="col-12 p-0">
      
      <h1 class="h3 mb-4">トークン編集</h1>

            <%= render 'form', token: @token %>